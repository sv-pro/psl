apiVersion: v1
kind: Pod
metadata:
  name: webapp
  namespace: production
spec:
  containers:
    - name: nginx
      image: nginx:1.21
    - name: app
      image: myapp:2.0
    - name: sidecar
      image: envoy:1.18
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "1Gi"
      cpu: "1000m"